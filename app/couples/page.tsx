import CollectionPage from "../_components/CollectionPage";

export default function CouplesPage() {
  return <CollectionPage config={{ slug: "couples", title: "Couples", eyebrow: "Bound in faith", description: "Faith-centred pieces for couples, friendship and shared purpose.", image: "/campaign/couple-connection.png", imagePosition: "center 38%" }} />;
}
